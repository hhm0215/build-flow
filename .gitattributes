# Shell 스크립트는 Windows에서도 LF 유지 (Git Bash/WSL bash가 CRLF 읽으면 실패)
*.sh text eol=lf

# Gradle wrapper 스크립트도 LF
gradlew text eol=lf

# 일반 텍스트 파일은 OS 자동 변환
* text=auto

# 바이너리 파일은 변환 금지
*.jar binary
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.xlsx binary
*.zip binary
